{
  "name": "palette",
  "description": "Connect your AI assistant to Palette — query clusters, diagnose issues, manage infrastructure using natural language.",
  "version": "0.6.0",
  "author": {
    "name": "Spectro Cloud"
  },
  "repository": "https://github.com/spectrocloud/palette-agent-toolkit",
  "userConfig": {
    "host": {
      "type": "string",
      "title": "Palette host",
      "description": "Required to connect. Your Palette tenant URL, e.g. example.spectrocloud.com"
    },
    "api_key": {
      "type": "string",
      "title": "Palette API key",
      "description": "Required unless using an auth token. Create under User Menu > My API Keys.",
      "sensitive": true
    },
    "auth_token": {
      "type": "string",
      "title": "Palette auth token",
      "description": "JWT alternative to an API key — set one, not both.",
      "sensitive": true
    },
    "ca_file": {
      "type": "string",
      "title": "Custom CA file path",
      "description": "Optional. CA bundle path for a self-hosted Palette behind a private CA."
    },
    "allow_write": {
      "type": "boolean",
      "title": "Enable write tools",
      "description": "Turns on mutating tools (create_*, update_*, delete_*). Off by default.",
      "default": false
    },
    "allow_direct_ssh": {
      "type": "boolean",
      "title": "Enable direct-SSH edge tools",
      "description": "Turns on run_edge_command and the read-only edge SSH tools, which dial caller-supplied hosts with caller-supplied credentials. Off by default.",
      "default": false
    }
  }
}
